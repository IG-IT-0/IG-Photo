rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development-friendly: allow all reads/writes on emulator only.
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2099, 1, 1);
    }
  }
}
